<div class = "Comment Update">
  
  <h1>Update Comment</h1>
  
  <%= form_for([@board,@list,@card, @comment]) do |f| %>
    <%= render(:partial => 'form', :locals => {:f => f}) %>
    
    <div class = "form-buttons">
      <%= f.submit("Update Comment")%>
    </div>
  
  <%end%>
  
  

</div>